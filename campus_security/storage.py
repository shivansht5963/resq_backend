# storage.py — placeholder kept for import compatibility.
# All storage is now handled by Django's built-in FileSystemStorage
# configured via STORAGES['default'] in settings.py.
# No GCS / cloud storage is used.
